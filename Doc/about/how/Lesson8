=[about/how/Lesson7]


P1.=[core/id/acme_incorporated]

<p>=
P2.=[core/id/quake_incorporated]

And we reference our prior draft:

Note=these are like twitter handles.

fontmark=red
fontmark1={fontmark}
fontmark2={fontmark}

Commentary=Adding parties.  But we should reference them instead of typing or pasting.  That obviously saves time and reduces errors, but even more importantly, it means that we are connecting this use with that person. So if ever we want to know the history of that person, we can just look for things that link to them.  (By search.)

NextPage=Signature_P1